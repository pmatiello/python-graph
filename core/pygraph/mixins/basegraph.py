class basegraph( object ):
    """
    An abstract class intended as a common ancestor to all graph classes. This allows the user to test isinstance(X, basegraph) to determine if 
    the object is one of any of the python-graph main classes.
    """
    